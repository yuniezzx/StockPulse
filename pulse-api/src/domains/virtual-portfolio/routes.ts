/**
 * PLACEHOLDER: virtual-portfolio domain routes not yet implemented.
 * 虚拟仓查询入口（pulse-core 自动追踪写入，前端只读）。
 * 详见 docs/architecture.md §3 业务边界 / §4.4 outbox 原子写入。
 */
import type { FastifyInstance } from "fastify";

export async function virtualPortfolioRoutes(_app: FastifyInstance): Promise<void> {}
