/**
 * PLACEHOLDER: strategies domain routes not yet implemented.
 * 策略元数据与权重查询入口（策略 key 三层一致：core/api/web）。
 * 详见 docs/architecture.md §3 / AGENTS.md §1 命名约定。
 */
import type { FastifyInstance } from "fastify";

export async function strategiesRoutes(_app: FastifyInstance): Promise<void> {}
