/**
 * PLACEHOLDER: evaluations domain routes not yet implemented.
 * 策略校验结果查询入口（pulse-core evaluation/ 模块产出）。
 * 详见 docs/architecture.md §3 业务边界。
 */
import type { FastifyInstance } from "fastify";

export async function evaluationsRoutes(_app: FastifyInstance): Promise<void> {}
