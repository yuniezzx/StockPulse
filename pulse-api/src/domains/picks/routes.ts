/**
 * PLACEHOLDER: picks domain routes not yet implemented.
 * 选股结果查询入口（漏斗输出 → 早报 → 前端展示）。
 * 详见 docs/architecture.md §3 业务边界（pulse-api 职责）。
 */
import type { FastifyInstance } from "fastify";

export async function picksRoutes(_app: FastifyInstance): Promise<void> {}
