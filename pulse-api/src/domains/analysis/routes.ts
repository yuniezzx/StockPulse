/**
 * PLACEHOLDER: analysis domain routes not yet implemented.
 * 个股/板块分析数据查询入口（指标 + 形态 + 资金面）。
 * 详见 docs/architecture.md §3 业务边界（pulse-api 职责）。
 */
import type { FastifyInstance } from "fastify";

export async function analysisRoutes(_app: FastifyInstance): Promise<void> {}
