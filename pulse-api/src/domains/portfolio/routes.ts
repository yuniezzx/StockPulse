/**
 * PLACEHOLDER: portfolio domain routes not yet implemented.
 * 真实持仓管理入口（用户主动写入，属于圈 4 用户数据）。
 * 详见 docs/architecture.md §3 业务边界（pulse-api 写圈 4）。
 */
import type { FastifyInstance } from "fastify";

export async function portfolioRoutes(_app: FastifyInstance): Promise<void> {}
